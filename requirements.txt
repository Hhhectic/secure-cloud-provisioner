fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
azure-identity==1.15.0
azure-mgmt-resource==26.0.0
azure-mgmt-storage==21.0.0

# --- Streamlit GUI (streamlit run app.py) ---
# The AWS side of the GUI imports backend/, so its dependencies come from
# backend/requirements.txt rather than being pinned a second time here.
streamlit>=1.32.0
python-dotenv>=1.0.0

# --- Azure VM provisioning, which backend/az/ does not cover ---
azure-mgmt-network>=25.0.0
azure-mgmt-compute>=30.0.0
azure-storage-blob>=12.19.0
